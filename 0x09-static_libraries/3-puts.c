#include "main.h"
void _puts(char *str);
#include<stdio.h>
/**
 * main - writes a string to stdout
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
