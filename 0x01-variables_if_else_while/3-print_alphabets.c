#include <stdio.h>
/**
 * main - alphABET
 *
 * Return: Always 0
 */
int main(void)
{
	char ltu;

	for (ltu = 'a'; ltu <= 'z'; ltu++)
		putchar(ltu);
	for (ltu = 'A'; ltu <= 'Z'; ltu++)
		putchar(ltu);
			putchar('\n');
			return (0);
}
